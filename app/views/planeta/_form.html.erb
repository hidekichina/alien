<%= form_for(@planetum) do |f| %>
  <% if @planetum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@planetum.errors.count, "error") %> prohibited this planetum from being saved:</h2>

      <ul>
      <% @planetum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
