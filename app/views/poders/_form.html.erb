<%= form_for(@poder) do |f| %>
  <% if @poder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poder.errors.count, "error") %> prohibited this poder from being saved:</h2>

      <ul>
      <% @poder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
